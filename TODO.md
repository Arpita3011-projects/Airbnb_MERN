- [x] Inspect and fix `connect-mongo` MongoStore initialization + touchAfter option in `app.js`
- [x] Fix `store.on('error')` handler to use the actual error argument
- [x] Re-run the app (basic node start) to ensure no startup crashes


